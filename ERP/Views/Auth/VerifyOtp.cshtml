@model ERP.Models.VerifyOtp
@{
    ViewData["Title"] = "Verify OTP";
}

<div class="flex items-center justify-center min-h-screen">
    <div class="bg-white shadow-xl rounded-2xl w-full p-12">
        <div class="text-center mb-6">
            <i class="fa-solid fa-building text-indigo-600 text-5xl mb-4"></i>
            <h2 class="text-3xl font-bold text-indigo-600">Verify OTP</h2>
        </div>

        @using (Html.BeginForm("CheckVerifyOtp", "Auth", FormMethod.Post, new { @class = "space-y-6" }))
        {
            @Html.AntiForgeryToken()
            <div>
                <label class="block font-semibold text-md text-gray-700">OTP</label>
                @Html.TextBoxFor(
                         m => m.otp,
                         new { @class = "mt-1 block w-96 rounded-xl border border-gray-300 px-4 py-3 text-gray-900 placeholder-gray-400 shadow-sm focus:border-indigo-500 focus:ring focus:ring-indigo-200 focus:ring-opacity-50", placeholder = "Enter 6-digit OTP", type = "text", required = "required" }
                         )
                @Html.ValidationMessageFor(m => m.otp, "", new { @class = "text-red-500 text-sm mt-1" })
            </div>

            <div class="flex items-center justify-between">
                <a asp-controller="Auth" asp-action="ForgetPassword" class="text-sm text-indigo-600 hover:underline">Back</a>
            </div>

            <div>
                <button type="submit" class="w-full bg-indigo-600 text-white font-semibold py-3 rounded-xl shadow-md hover:bg-indigo-700 transition duration-200">
                    Verify OTP
                </button>
            </div>
        }
    </div>
</div>